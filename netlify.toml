[build]
  command = "npm run build"
  publish = "dist"

# Le fichier _redirects dans public/ gère les redirections
# Les fichiers markdown sont exclus de la redirection SPA

[build.environment]
  NODE_VERSION = "18"

